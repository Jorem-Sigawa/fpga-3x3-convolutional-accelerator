                 ┌────────────────────────────┐
                 │          Vitis C code       │
                 │   running on ARM processor  │
                 └──────────────┬─────────────┘
                                │
                                │ configures DMA registers
                                │
┌──────────────┐        ┌───────▼────────┐        ┌─────────────────┐
│ DDR memory   │ ─────► │ AXI DMA MM2S   │ ─────► │ your accelerator │
│ input image  │        │ memory→stream  │        │ line buffer/MAC  │
└──────────────┘        └────────────────┘        └────────┬────────┘
                                                            │
┌──────────────┐        ┌────────────────┐                  │
│ DDR memory   │ ◄───── │ AXI DMA S2MM   │ ◄────────────────┘
│ output image │        │ stream→memory  │
└──────────────┘        └────────────────┘

3  4  5
8  9  10
13 14 15

4  5  6
9  10 11
14 15 16

Datapath:
    line buffers
    shift registers / window registers
    MAC units
    counters
    output registers

Control:
    idle/start state
    valid_in handling
    count/current_height updates
    valid_out timing
    frame_done timing
    reset behavior